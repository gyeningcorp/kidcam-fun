import 'package:flutter/material.dart';

/// KidCam Fun color palette — bright, playful, and kid-friendly.
abstract final class AppColors {
  // Primary palette
  static const sunshineYellow = Color(0xFFFFD54F);
  static const skyBlue = Color(0xFF64B5F6);
  static const coral = Color(0xFFFF8A65);
  static const softPurple = Color(0xFFCE93D8);
  static const mintGreen = Color(0xFF81C784);

  // UI colors
  static const textDark = Color(0xFF37474F);
  static const textLight = Color(0xFF78909C);
  static const background = Color(0xFFF5F5F5);
  static const white = Color(0xFFFFFFFF);

  // Filter carousel
  static const activeFilterRing = Color(0xFFFFFFFF);
  static const inactiveFilterBg = Color(0x33000000);

  // Parent gate
  static const parentGateBg = Color(0xFFE8EAF6);
  static const cooldownRed = Color(0xFFEF5350);

  // Capture button
  static const captureOuter = Color(0xFFFF7043);
  static const captureInner = Color(0xFFFFFFFF);

  // Gradients
  static const splashGradientTop = Color(0xFF64B5F6);
  static const splashGradientBottom = Color(0xFFCE93D8);
  static const homeGradientTop = Color(0xFFFFF3E0);
  static const homeGradientBottom = Color(0xFFE8F5E9);
}
